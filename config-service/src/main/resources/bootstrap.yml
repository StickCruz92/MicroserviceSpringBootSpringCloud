server:
 port: 8084

spring:  cloud:
   config:      server:        git:          uri: https://github.com/StickCruz92/MicroserviceSpringBootSpringCloud.git
          searchPath: config-data
          username: ${GIT_USER}
          passwork: ${GIT_PASSWORD}
   security: 
    user:    
     name: root
     password: secrect
 